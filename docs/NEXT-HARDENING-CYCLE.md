# Next hardening cycle

Checkpoint: 2026-09-20. **HIGH-COST WORK PAUSED by Houssein. Demo stopped.**

When Houssein's weekly high-performance allowance resets, read this file and docs/final-stabilization.md first. Resume from the remaining blockers/backlog. Do not repeat completed audits or rebuild context unnecessarily.

Resume only when Houssein explicitly requests it; a budget reset alone is not authorization. Until then, make only requested small fixes with the smallest safe change and targeted verification. No autonomous hardening, broad scans/audits, full suites, infrastructure setup, architecture work or Playwright sweeps. Use Playwright only for a specifically requested check. Update this checkpoint if a small fix creates follow-up, then stop.

## 1. Current state

- **NOT READY for production sign-off.** Houssein considers the system usable enough for current work. Remaining evidence/security gates below must not be confused with failing functional regression.
- Completed hardening covers receiving/receipt accumulation, canonical physical totals, warehouse stock, existing-plus-new container capacity, assignment selection/eligibility, state protection, orders/containers, shipments/tracking, buyers, imports/exports, search/pagination, authorization, concurrency/idempotency, rollback and audit. Do not reopen passed domains without a directly affecting change. Detailed baseline: [production-hardening.md](production-hardening.md); latest decisions supersede older unanswered questions in [final-stabilization.md](final-stabilization.md).
- Additional completed work: stale-write protection for catalog/admin edits; atomic arrival notifications including day zero; safe legacy-template measurement handling; owner incidents/dashboard, encrypted credential recovery and MFA controls; database-bound session/version enforcement; local retention/encrypted-backup tools; explicit disabled-integration UI. Owner security is **locally tested, not activated in production**.
- Latest final results (2026-09-20): **59/59 PHP suites, 15/15 JS suites, 316/316 syntax checks (257 PHP, 59 JS)**; HTTP security and persistent headed critical-workflow/owner QA passed. Includes permissions, concurrency, idempotency, rollback, import/export and finalized-record protections. Final test-only midnight fixture correction uses persisted dates. No tests were rerun for this checkpoint.
- Performance already achieved: warehouse count previously exceeded 90 seconds (bounded reproduction also exceeded 3 seconds), now list requests 38/52/54 ms. Orders list 628/641/656 ms and 1,294,835 bytes became 362/370/375/393 ms and 70,604 bytes: **94.5% smaller, approximately 42% lower median latency**. No stale business-critical cache. See [Measured performance](final-stabilization.md#measured-performance). Later under-test-load samples are observations, not comparable optimization claims.
- Staff UX already implemented: useful text/numeric selection while editing, keyboard/mouse editing coverage, duplicate-submit guards, stale/late-response protection, assignment selection surviving repaint, preserved warehouse filters/search across reload/Back, request-error recovery, truthful disabled-tracking confirmations, owner incident navigation and automatically hidden credentials. See [Fixes and downstream verification](final-stabilization.md#fixes-and-downstream-verification).
- Large **uncommitted working tree** remains; no hardening commit was made. At checkpoint, 128 tracked paths were modified; additional new files exist (83 untracked paths within docs/services/tests/scripts alone). These include prior work, not just this checkpoint. Preserve all changes; do not reset/overwrite. File groups: [Closure files grouped by affected control](final-stabilization.md#closure-files-grouped-by-affected-control) and earlier modified-file section.
- Source business-table checksums were unchanged. Approved source mutations: additive migrations/control tables, 654 preserved historical flags, five recovery-required user flags, 49 audited zero-reference translation-job deletions. No historical cargo values, account roles/passwords/session versions or financial facts were changed. Jobs 6/7 were retained due to cache references.
- QA database, owned temporary credentials/keys/config/sessions/logs and three QA servers were removed. The QA browser context closed; original staff browser was left on Containers. Do not assume a future session has live handles. Workspace: `C:/xampp/htdocs/cargochina`; PHP: `C:/xampp/php/php.exe`; source DB: `clms`. Ignored QA evidence is under `artifacts/cargo-demo/`; do not rerun old mutation/report helpers blindly or overwrite baseline evidence.

## 2. Remaining release blockers

The authoritative current sections are [Approved decisions and closure implementation](final-stabilization.md#approved-decisions-and-closure-implementation---2026-09-19) through [QA teardown and remaining activation gates](final-stabilization.md#qa-teardown-and-remaining-activation-gates). Older inventories contain superseded questions; D1-D5 were answered.

| Gate | Exact remaining decision/evidence |
|---|---|
| Owner identity | Confirm Houssein personally owns/controls account **1**, `admin@salameh.com / Super Admin`, and verify emergency access. Generic name/activity is not identity proof. No automatic promotion. |
| Responsibilities | **A3 ChinaAdmin, A5 LebanonAdmin, A6 ChinaEmployee/FieldStaff remain PENDING.** Identify verified named future owners; preserve historical actors. **A4 approved: Cherry160**, warehouse receiving/operations only; existing permissions suffice, no extra grant. A11 replacement depends on verified account1; do not disable/revoke11 prematurely. See [Legacy responsibility mapping and credential status](final-stabilization.md#legacy-responsibility-mapping-and-credential-status). |
| Historical reconciliation | **654 records**, original snapshots preserved with `HISTORICAL_RECONCILIATION_REQUIRED`. Houssein approves; warehouse supplies signed receiving/packing evidence; accounting supplies approved numbering map. Item55/order28: **168 ordered / 180 received**. Unallocated receipts28/order24,30/order34,34/order23,211-215/orders734-738; finalized734-738 require evidence plus explicit approval. Duplicate buyer16 AS-1 (items68/94, orders34/52); buyer17 CUARRi (items229-743, order170). Missing measurement basis:634 order items and12 template items. Never invent historical values or renumber without evidence. Grandfather only proven no current stock/money/shipment/numbering impact. Exact rows: **Consolidated historical reconciliation table** in the source report; do not duplicate it. |
| Terminology/classification | Houssein approves the existing **Compact classification / terminology decision table** and exact product/item IDs. Release uses manual/approved translations. `grg`/`rygergh` are INVALID/UNKNOWN, not invented translations; grg still references product878/item1080/description entry926. Jobs120/122 flagged;49 approved jobs removed,6/7 retained. See [Classification and translation](final-stabilization.md#classification-and-translation). |
| Five seed credentials | Accounts **3,4,5,6,11** remain active, original known/default passwords, session_version0; recovery-required flags set, source escrow rows0. After owner recovery is safely operational, rotate once into encrypted escrow and revoke old sessions under the approved mapping/access conditions. Never print/send/store plaintext credentials. |
| Escrow/MFA/security provisioning | Provision protected AES-GCM keyring outside DB/webroot, independent owner authenticator factor, custody/recovery and verified owner allowlist. Require recent sign-in, current password and replay-protected MFA for reveals. Production owner config unset. Verify HTTPS and OS/application/database key isolation; process-owner inspection was denied. Global legacy-session policy activation remains pending. [Owner controls and security boundary](final-stabilization.md#owner-controls-and-security-boundary); configuration contract: [owner-controls.md](owner-controls.md). |
| Production DB privilege | Current **root@localhost** fails escrow's restricted-grant boundary. Establish least-privilege database/service identities and prove ACL/key separation; do not silently change deployment credentials. |
| Immutable audit | Provision and verify real append-resistant/off-host audit delivery, signed acknowledgement, retention and failure behavior. Disposable signed local stub proved only client behavior, not immutability. Recovery must fail closed without durable acknowledgement. |
| Off-host backup | **No destination exists. OFF-HOST BACKUP EVIDENCE REQUIRED.** Proposed company-controlled Backblaze B2 bucket; encrypted before upload, restricted non-destructive job credential, separate Houssein admin custody, Object Lock/append-resistant audit. Do not invent bucket/credentials. |
| Recovery proof | Real **backup -> encrypted off-host upload -> independent download -> isolated decrypt/restore -> application/data verification**, including uploads/configuration/key recovery. Local encrypted SQL rehearsal:74 tables, zero checksum mismatches,5.601 seconds; not whole-installation/off-host proof. |
| Retention/RPO/RTO | Prove scheduler, holds, generations and production-style **RPO <=4h / RTO <=4h**. Approved: cargo/financial/linked uploads10y; audit/referenced departed identities7y; application/security logs1y; debug/temporary exports90d; expired session material90d; backups35daily+12monthly+3annual (retain four-hour snapshots within35d). Holds override until Houssein releases. Undated logs/unowned uploads/shared sessions need ownership/date evidence; no production purge ran. See [Approved retention and infrastructure evidence](final-stabilization.md#approved-retention-and-infrastructure-evidence). |
| Other manual/external evidence | Verify intended PHP/database/business timezone at deployment; do not convert historical timestamps speculatively. Tracking/email/WhatsApp may remain disabled with documented manual process; live provider tests are **EXTERNAL VERIFICATION REQUIRED only before enabling**, not internal-release blockers. No real sends without authorization. Whole-server outage/offline owner alerts need external monitoring evidence; local badge cannot provide that. |

## 3. Owner control requirements

- Houssein needs one central incident/error dashboard and one secure view of all internal employee accounts. Implemented owner UI must be securely activated, not rebuilt.
- Meaningful failures must trace **user -> action/workflow -> affected record -> audit history**, with request ID, safe error category/context, rollback/retry evidence, first/last occurrence, affected users, count, severity, resolution status and notes.
- Separate system defects/infrastructure/security failures from expected validation, incorrect workflow and training issues. Deduplicate by fingerprint. Critical/High incidents need immediate high visibility without repeated notification spam; existing visible owner badge is local, not offline/outage delivery.
- Eligible existing employee passwords must be recoverable through **authenticated encrypted escrow**, never plaintext DB storage. Authentication continues using the one-way hash; encryption keys remain outside SQL. Ordinary APIs/lists/exports/logs must expose neither ciphertext nor plaintext.
- Reveals require **verified owner allowlisting plus current SuperAdmin**, recent reauthentication, current password and independent MFA; durable audited actor/target/time/session/IP metadata without secrets. Clear revealed values after20 seconds/blur/navigation; fail closed on audit or authorization failure. Preserve targeted authorization/redaction/tamper/replay/concurrency/UI tests.

## 4. Next high-performance cycle

Only after Houssein asks to resume:

1. Close remaining release blockers; classify externally dependent gates without inventing proof.
2. Provision security/credential controls.
3. Configure and prove off-host backup/restore.
4. Resolve outstanding owner/business decisions. Obtain any prerequisite decision before its dependent mutation, even if listed earlier.
5. Run the final full regression **once after all resolvable changes**; use targeted tests during fixes.
6. Perform final downstream impact verification across affected logic/DB/API/calculations/pages/filters/imports/exports/permissions/statuses/workflows/audit/integrations. New defects invalidate sign-off; verify their fixes appropriately.
7. Only then address deeper UI/UX, workflow and performance polishing based on evidence/backlog. Do not claim READY if later changes remain unverified.
8. Finally resume the recorded demo when release gates and verification permit and Houssein authorizes resumption.

Do not re-audit completed domains unless a later change directly affects them. Use disposable/rollback-safe fixtures, no real notifications or external financial/customer actions. Preserve source evidence and uncommitted work.

## 5. Future polishing backlog

Latest explicitly requested MySQL5.5 migration adaptation (supersedes the earlier index-blocker status below): **083** retains full utf8mb4 255-character paths with a 32-byte SHA-256 primary key, server-maintained insert/update triggers and a non-unique path lookup prefix; existing rows can be upgraded without truncation. A hash collision rejects an insert rather than allowing duplicate ownership. **084** indexes only the first128 characters of the non-unique retention reference while retaining/comparing its full value. **085** uses one automatic timestamp plus an insert trigger for last_seen, respecting MySQL5.5's one-automatic-timestamp limit. **086** retains the conditional column addition. Existing JSON CHECK declarations remain effective on supporting engines; MySQL5.5 ignores CHECK and still cannot execute the application's JSON queries.

Run the complete updated files **083 -> 084 -> 085 -> 086** in production phpMyAdmin with `clms` selected, during a maintenance window: 083/085 create/recreate named triggers and require TRIGGER privilege (and applicable binary-log trigger privileges). SHA2 support is required for upload digests. No global InnoDB settings are changed. Do not run just the CREATE TABLE portions or resume writes after a failed trigger statement. No production SQL was executed here. Actual MySQL5.5 execution remains unverified; this is schema-installation work, not an application JSON backport or production-ready claim.

Targeted regression: `C:/xampp/php/php.exe tests/migrations_083_086_compatibility_test.php` passes on local MariaDB10.4 using COMPACT row format to reproduce the767-byte key ceiling. Covers fresh install, repeated/partial/old-schema migration, full-length Unicode paths, duplicate rejection, common-prefix distinct paths, rename digest maintenance, upload ownership FK, exact full retention reference lookup, incident timestamps and preserved user values. Disposable schema removed; source unchanged. PHP syntax check passed. No full suite or browser run; high-cost pause continues.

Production version now confirmed by screenshot: **MySQL 5.5.62-log**, `innodb_file_format=Antelope`, `innodb_large_prefix=OFF`; visible users.id is unsigned INT. This supersedes the pending-version question below. **An index-only migration edit is insufficient:** existing `UploadAccessService` runtime authorization uses JSON_VALID/JSON_SEARCH/JSON_EXTRACT, unavailable on MySQL5.5. Do not claim 083-086/application compatibility with this server or weaken authorization to make installation succeed. A separately authorized database-version migration with isolated restore verification is required, or an explicitly scoped application backport; neither was started during the usage-budget pause. Earlier local checks were MariaDB10.4, not production-version proof.

Production follow-up: phpMyAdmin screenshot shows **083 fails with #1071, maximum key length 767 bytes**. Earlier local compatibility tests did not reproduce that server limit. `upload_assets.path` is a full `utf8mb4 VARCHAR(255)` primary key (up to1020 bytes); 084's retention scope composite index also exceeds767 bytes. **Production compatibility remains unresolved** pending read-only server version/InnoDB settings and `SHOW CREATE TABLE users`. Do not shorten stored paths, use a non-unique replacement for the ownership primary key, or assume large-prefix/row-format support. No production settings/data were changed.

Follow-up requested compatibility fix: migrations **084, 085 and 086** now use the same MySQL-compatible conditional column checks; **083 already uses compatible `CREATE TABLE IF NOT EXISTS` and required no edit**. Targeted local MariaDB disposable-schema checks passed for all four: fresh installation, rerun, partial deployment, existing-value preservation, table creation and upload ownership foreign key. Source database unchanged; disposable schema removed. Remote MySQL execution/version and its CHECK-constraint enforcement remain unverified; no full suite or broad audit was run.

Requested small fix after checkpoint: migration `082_order_template_metrics.sql` used MariaDB-only `ADD COLUMN IF NOT EXISTS`, rejected by the reported MySQL server (#1064). All three additions in that file now check `information_schema.COLUMNS` and use prepared conditional ALTER statements. Targeted disposable-schema verification on local MariaDB covers fresh installation, rerun, partial deployment and existing-value preservation; source data unchanged. **Actual remote MySQL/phpMyAdmin rerun remains to be confirmed.** No full suite, browser check or other migration audit was run; high-cost pause remains in effect.

Collect actual employee reports during normal work; no proactive hunt during the budget pause. Record workflow/record reference and safe reproduction context, never passwords or sensitive payloads. Unknown frequency stays unknown. Release blockers above are not duplicated here.

| Area | Issue/friction | Severity | Frequency | Proposed improvement | Requires high-performance cycle? |
|---|---|---|---|---|---|
| Staff feedback intake | No additional verified staff-friction report recorded at checkpoint | Unrated | Unknown | Append concrete employee reports; prioritize repeated/blocking friction | Triage only on request; small safe fixes may use targeted verification |

Do not turn this placeholder into an invented defect list. For each requested small fix, update follow-up here only if needed, perform targeted verification, and stop.
